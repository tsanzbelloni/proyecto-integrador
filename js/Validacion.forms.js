window.addEventListener('load', function(){ 

let formulario = document.querySelector('form');
let Busqueda = docuement.querySelector('.Buscar');

// Cuando la creacion de la variable Busqueda no esta comentada, el preventDefault no hace efecto



formulario.addEventListener('submit', function(e){
    e.preventDefault();

})






})